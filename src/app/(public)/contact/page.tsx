import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact Us | LibSphere',
  description: 'Get in touch with Baba Vishwakarma Public Library support. Reach out with questions, book donation offers, or membership queries.',
};

export default function ContactPage() {
  return <ContactClient />;
}
